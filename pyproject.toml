[project]
name = "connect-four-cpp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "setuptools>=82.0.1",
    "numpy",
    "torch",
    "tqdm",
]
